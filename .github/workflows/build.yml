name: Build

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  Build:
    uses: microsoft/digitalworkplace-workflows/.github/workflows/build.yml@users/mibir/new-workflows
    with:
     project-to-build: "QueryBuilder/QueryBuilder.csproj"
     project-to-test: "QueryBuilder.Test/QueryBuilder.Test.csproj"